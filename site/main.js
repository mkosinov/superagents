// Данные гейтов конвейера. Источник: docs/workflow/design-phase.md, impl-phase.md
// и актуальная автоматизация memo (auto-design / auto-impl watchers, board fields).
const GATES = {
  ga: {
    badge: "A",
    title: "Концепция — авто, если нет развилки",
    text: "Наблюдатель берёт свободную карточку из Backlog — сначала из явной очереди Next Up. Read-only скаут проверяет задачу против живого кода: каждое утверждение со ссылкой на файл и строку, плюс проверка актуальности — устаревшая задача закрывается с доказательствами, а не реализуется вслепую. Дальше два-три подхода с ценой каждого. Не различаются для пользователя, данных и контракта — концепция выбирается автоматически, без остановки. Развилка в поведении или границах — на карточке появляется метка gate: concept, конвейер ждёт человека.",
    facts: ["разведка: read-only скаут", "устаревшие задачи закрываются с доказательствами", "стоп — только на реальной развилке"],
  },
  gb: {
    badge: "B",
    title: "Спека + панель из шести ревьюеров",
    text: "Спека самодостаточная, с двумя обязательными разделами: сценарии пользователя (3–7, каждый превращается в E2E-тест) и дельта поведения — что меняется для пользователя, до и после. Шесть независимых агентов параллельно ревьюят: полнота, согласованность, осуществимость, простота, лучшие практики, безопасность. Сводка одна: повторы убраны, замечания ранжированы BLOCKER > MAJOR > MINOR, каждое проверено по коду. Единственный обязательный стоп фазы: человек читает и говорит «ок» — до этого пуша нет.",
    facts: ["6 параллельных перспектив", "ранжирование BLOCKER > MAJOR > MINOR", "единственный обязательный стоп фазы"],
  },
  gc: {
    badge: "C",
    title: "План — авто-OK, если спека цела",
    text: "План разбивает фичу на задачи с классом trivial / small / standard / large — класс определяет бюджет ревью в IMPL. Каждый якорь задачи привязан к сценарию пользователя. Агент plan-reviewer сверяет план со спекой. Ревью не трогает спеку — правки вносятся в план, пуш, карточка переходит в Ready to IMPL, и IMPL-наблюдатель подхватывает её сам. Вскрылось, что спека должна измениться — стоп с меткой gate: plan, обсуждение возвращается к человеку.",
    facts: ["классы задач: trivial / small / standard / large", "каждая задача → сценарий пользователя", "стоп — только при изменении спеки"],
  },
  g3: {
    badge: "G3",
    title: "Worktree и зелёный baseline",
    text: "IMPL стартует с готового плана в main. Контейнерный менеджер проверяет, что карточка в статусе Ready to IMPL, заводит отдельный git worktree под фичу и убеждается, что базовые тесты зелёные до начала работы. Красный baseline — блокировка, а не «починим по ходу».",
    facts: ["вход: карточка Ready to IMPL", "изоляция: git worktree", "красный baseline = блок"],
  },
  g46: {
    badge: "G4–G6",
    title: "Цикл задач: TDD + двухступенчатое ревью",
    text: "Задачи исполняются строго последовательно. Кодер (frontend или backend) пишет по TDD: красный тест, реализация, рефакторинг. После задачи — ревью по классу задачи: trivial — самопроверка архитектора, small — только соответствие плану, standard и large — полное двухступенчатое: сначала код соответствует плану, затем качество кода и прогон тестов. Diff передаётся ревьюеру файлом, а не текстом в промпте. Максимум три цикла правок на ревьюера — дальше стоп и эскалация человеку.",
    facts: ["TDD: RED → GREEN → REFACTOR", "двухступенчатое ревью", "предохранитель: 3 цикла", "diff файлом, не промптом"],
  },
  g45: {
    badge: "G4.5",
    title: "Визуальный гейт: скриншоты + DOM",
    text: "Для фаз, меняющих пользовательский интерфейс — один прогон на всю фазу. Скрипт снимает скриншоты ключевых состояний страниц (мобильный размер по умолчанию) и проверяет, что DOM-элементы из чек-листа спеки существуют и видимы. Ловит то, что юнит-тесты обычно пропускают: не те вкладки, отсутствующие кнопки, поехавшую вёрстку. Провал — мягкая блокировка: человек решает чинить, пропустить или откатить.",
    facts: ["Playwright-скриншоты", "чек-лист из спеки", "только для UI-фаз"],
  },
  docs: {
    badge: "docs",
    title: "Документация в ветке фичи",
    text: "До мержа агент-редактор обновляет PLAN.md и CHANGELOG.md по контексту фичи: спека, план, задачи, тесты. Документация едет тем же коммитом, что и код, — ветка попадает в main уже документированной, а не «допишем потом».",
    facts: ["исполнитель: @docser", "PLAN.md + CHANGELOG.md", "коммит — в ветку фичи"],
  },
  g7: {
    badge: "G7",
    title: "Финал: выбор за человеком",
    text: "Финальный прогон полного набора тестов, затем человек выбирает одно из четырёх: смержить локально, открыть PR (путь по умолчанию), оставить ветку на потом или отбросить работу с подтверждением. Карточка уходит в PR (G7) и после мержа — в In-main; очередь Next Up сдвигается сама. Мелкая полировка идёт по облегчённому протоколу — без нового цикла спека-план.",
    facts: ["merge / PR / keep / discard", "финальный прогон test:all", "после мержа очередь сдвигается сама"],
  },
};

const detailEl = document.getElementById("gate-detail");
const badgeEl = document.getElementById("detail-badge");
const titleEl = document.getElementById("detail-title");
const textEl = document.getElementById("detail-text");
const factsEl = document.getElementById("detail-facts");
const gateButtons = document.querySelectorAll(".gate");

function showGate(key) {
  const g = GATES[key];
  if (!g) return;
  badgeEl.textContent = g.badge;
  titleEl.textContent = g.title;
  textEl.textContent = g.text;
  factsEl.innerHTML = "";
  for (const f of g.facts) {
    const span = document.createElement("span");
    span.textContent = "· " + f;
    factsEl.appendChild(span);
  }
  gateButtons.forEach((b) => b.classList.toggle("active", b.dataset.gate === key));

  // панель может быть ниже зоны клика (особенно на мобильном): сразу подтянуть к экрану
  const rect = detailEl.getBoundingClientRect();
  if (rect.top < 0 || rect.bottom > window.innerHeight) {
    detailEl.scrollIntoView({ behavior: "instant", block: "center" });
  }
  // перезапуск подсветки, чтобы смена карточки была заметна
  detailEl.classList.remove("flash");
  void detailEl.offsetWidth;
  detailEl.classList.add("flash");
}

gateButtons.forEach((b) => b.addEventListener("click", () => showGate(b.dataset.gate)));

// стартовая карточка — единственный обязательный стоп человека
showGate("gb");
